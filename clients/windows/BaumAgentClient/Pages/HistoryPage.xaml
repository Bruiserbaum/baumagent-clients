<Page
    x:Class="BaumAgent.Pages.HistoryPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Background="{StaticResource BaumBackgroundBrush}">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Spacing="12"
                    Margin="0,0,0,16" VerticalAlignment="Center">
            <TextBlock Text="History" FontSize="24" FontWeight="Bold"
                       Foreground="{StaticResource BaumTextBrush}" VerticalAlignment="Center"/>
            <Button x:Name="RefreshBtn" Content="↻ Refresh" Click="Refresh_Click"
                    Padding="10,5" FontSize="12"/>
            <TextBlock x:Name="StatusText" Text="" FontSize="13"
                       Foreground="{StaticResource BaumSubtextBrush}" VerticalAlignment="Center"/>
        </StackPanel>

        <!-- Projects section -->
        <Border Grid.Row="1" x:Name="ProjectsPanel" Visibility="Collapsed"
                Style="{StaticResource CardStyle}" Margin="0,0,0,12">
            <StackPanel Spacing="10">
                <TextBlock Text="Projects" FontWeight="SemiBold"
                           Foreground="{StaticResource BaumTextBrush}"/>
                <ListView x:Name="ProjectsList" SelectionMode="None" Background="Transparent"
                          IsItemClickEnabled="True" ItemClick="Project_Click">
                    <ListView.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapGrid Orientation="Horizontal" MaximumRowsOrColumns="6"/>
                        </ItemsPanelTemplate>
                    </ListView.ItemsPanel>
                    <ListView.ItemContainerStyle>
                        <Style TargetType="ListViewItem">
                            <Setter Property="Padding" Value="0"/>
                            <Setter Property="Margin" Value="0,0,8,8"/>
                            <Setter Property="Background" Value="Transparent"/>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="ListViewItem">
                                        <ContentPresenter/>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </ListView.ItemContainerStyle>
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <Border CornerRadius="8" Padding="14,10" MinWidth="140"
                                    Background="{Binding AccentColor}">
                                <StackPanel Spacing="4">
                                    <TextBlock Text="{Binding Name}" FontWeight="SemiBold"
                                               FontSize="14" Foreground="White"
                                               TextWrapping="NoWrap" TextTrimming="CharacterEllipsis"/>
                                    <TextBlock Text="{Binding TaskCountLabel}" FontSize="12"
                                               Foreground="#ccffffff"/>
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </StackPanel>
        </Border>

        <!-- Finished tasks header -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" Spacing="12"
                    Margin="0,0,0,8" VerticalAlignment="Center">
            <TextBlock Text="Finished Tasks" FontWeight="SemiBold" FontSize="15"
                       Foreground="{StaticResource BaumTextBrush}" VerticalAlignment="Center"/>
            <TextBlock x:Name="FilterLabel" Text="" FontSize="12"
                       Foreground="{StaticResource BaumSubtextBrush}" VerticalAlignment="Center"/>
            <Button x:Name="ClearFilterBtn" Content="Show all" Visibility="Collapsed"
                    Click="ClearFilter_Click" Padding="8,3" FontSize="12"/>
        </StackPanel>

        <!-- Task list -->
        <ListView x:Name="TasksList" Grid.Row="3"
                  SelectionMode="None" Background="Transparent"
                  ItemClick="Task_Click" IsItemClickEnabled="True">
            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                    <Setter Property="Padding" Value="0,3"/>
                    <Setter Property="Background" Value="Transparent"/>
                </Style>
            </ListView.ItemContainerStyle>
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Border Style="{StaticResource CardStyle}" Padding="14,10">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <!-- Status badge -->
                            <Border Grid.Column="0" CornerRadius="4" Padding="6,2"
                                    Background="{Binding StatusBg}" Margin="0,0,10,0"
                                    VerticalAlignment="Center">
                                <TextBlock Text="{Binding StatusLabel}" FontSize="10"
                                           FontWeight="Bold" Foreground="{Binding StatusFg}"/>
                            </Border>

                            <!-- Type badge -->
                            <Border Grid.Column="1" CornerRadius="4" Padding="6,2"
                                    Background="{Binding TypeBg}" Margin="0,0,12,0"
                                    VerticalAlignment="Center">
                                <TextBlock Text="{Binding TypeLabel}" FontSize="10"
                                           FontWeight="Bold" Foreground="{Binding TypeFg}"/>
                            </Border>

                            <!-- Description -->
                            <TextBlock Grid.Column="2" Text="{Binding Description}"
                                       Foreground="{StaticResource BaumTextBrush}"
                                       FontSize="13" TextTrimming="CharacterEllipsis"
                                       VerticalAlignment="Center"/>

                            <!-- Date -->
                            <TextBlock Grid.Column="3" Text="{Binding DateLabel}"
                                       Foreground="{StaticResource BaumSubtextBrush}"
                                       FontSize="12" VerticalAlignment="Center" Margin="12,0,0,0"/>
                        </Grid>
                    </Border>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>
