<Page
    x:Class="BaumAgent.Pages.TaskListPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Background="{StaticResource BaumBackgroundBrush}">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Grid Grid.Row="0" Margin="0,0,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock Text="Tasks" FontSize="24" FontWeight="Bold"
                       Foreground="{StaticResource BaumTextBrush}" VerticalAlignment="Center"/>
            <Button Grid.Column="1" Content="Refresh" Click="Refresh_Click"/>
        </Grid>

        <!-- Queue status bar -->
        <Border Grid.Row="1" Style="{StaticResource CardStyle}" Margin="0,0,0,12" Padding="12,8">
            <StackPanel Orientation="Horizontal" Spacing="16">
                <TextBlock x:Name="QueuedCount" Text="Queued: 0"
                           Foreground="{StaticResource BaumSubtextBrush}" FontSize="13"/>
                <TextBlock x:Name="RunningCount" Text="Running: 0"
                           Foreground="{StaticResource BaumAccentBrush}" FontSize="13"/>
                <TextBlock x:Name="TotalCount" Text="Total: 0"
                           Foreground="{StaticResource BaumSubtextBrush}" FontSize="13"/>
            </StackPanel>
        </Border>

        <!-- Refresh error banner (hidden unless a load/refresh fails) -->
        <TextBlock x:Name="RefreshErrorText" Grid.Row="2" Text=""
                   Visibility="Collapsed" Margin="0,0,0,8"
                   Foreground="{StaticResource BaumDangerBrush}" FontSize="12"
                   TextWrapping="Wrap"/>

        <!-- Task list -->
        <ListView x:Name="TaskListView" Grid.Row="3"
                  SelectionMode="Single"
                  SelectionChanged="TaskList_SelectionChanged"
                  Background="Transparent">
            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                    <Setter Property="Padding" Value="0,4"/>
                </Style>
            </ListView.ItemContainerStyle>
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Border Style="{StaticResource CardStyle}" Padding="14,12">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel Spacing="4">
                                <TextBlock Text="{Binding Description}" FontWeight="SemiBold"
                                           Foreground="{StaticResource BaumTextBrush}"
                                           TextTrimming="CharacterEllipsis" MaxLines="2"/>
                                <StackPanel Orientation="Horizontal" Spacing="8">
                                    <TextBlock Text="{Binding TaskType}" FontSize="11"
                                               Foreground="{StaticResource BaumSubtextBrush}"/>
                                    <TextBlock Text="·" FontSize="11"
                                               Foreground="{StaticResource BaumSubtextBrush}"/>
                                    <TextBlock Text="{Binding CreatedAt}" FontSize="11"
                                               Foreground="{StaticResource BaumSubtextBrush}"/>
                                </StackPanel>
                            </StackPanel>
                            <Border Grid.Column="1" CornerRadius="4" Padding="8,3" VerticalAlignment="Center">
                                <Border.Background>
                                    <SolidColorBrush Color="{Binding StatusColor}"/>
                                </Border.Background>
                                <TextBlock Text="{Binding Status}" FontSize="11" FontWeight="SemiBold"
                                           Foreground="White"/>
                            </Border>
                        </Grid>
                    </Border>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

        <!-- Pagination -->
        <StackPanel Grid.Row="4" Orientation="Horizontal" Spacing="8"
                    HorizontalAlignment="Center" Margin="0,12,0,0">
            <Button x:Name="PrevPage" Content="← Prev" Click="PrevPage_Click"/>
            <TextBlock x:Name="PageLabel" VerticalAlignment="Center"
                       Foreground="{StaticResource BaumSubtextBrush}"/>
            <Button x:Name="NextPage" Content="Next →" Click="NextPage_Click"/>
        </StackPanel>
    </Grid>
</Page>
